1. looks like we are using Kivy
  a. clone git repo
  b. make in the repo directory
  c. install kivy-designer for lulz.

install kivy by first downloading the repo, then building
1. git clone git://github.com/kivy/kivy.git
2. cd kivy
3. make
